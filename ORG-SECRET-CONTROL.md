Control probe -- PUBLIC repo, same Free org as wc-contact-worker.

If step 3 "Create GitHub App installation token" succeeds here but skipped
there, the blocker is the Free plan excluding PRIVATE repos from org
secrets, not broken org credentials.

Throwaway. Close the PR and delete the branch once the answer is read.
